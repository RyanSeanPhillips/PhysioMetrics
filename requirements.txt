# Core GUI Framework
PyQt6>=6.4.0

# Scientific Computing
numpy>=1.21.0
pandas>=1.5.0
scipy>=1.9.0
scikit-learn>=1.0.0  # For GMM breath clustering
numba>=0.57.0  # Optional: 10-50× speedup for breath detection (highly recommended)

# Plotting
matplotlib>=3.6.0
pyqtgraph>=0.13.0  # High-performance plotting backend (GPU-accelerated, 10-50× faster for large datasets)

# File format loaders
# neo>=0.12.0  # Uncomment if using neo for ABF files
pyabf>=2.3.0  # For ABF file support
pyedflib>=0.1.30  # For EDF/EDF+ file support (requires numpy<2.0)
python-docx>=1.0.0  # For scanning Word .docx files in notes
openpyxl>=3.0.0  # For scanning Excel .xlsx files in notes

# Telemetry and analytics (optional)
requests>=2.28.0  # For Google Analytics 4 integration

# Additional dependencies that may be needed
pathlib2>=2.3.0  # For older Python compatibility (optional)

# Build tool for creating executable
pyinstaller>=5.7.0